services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: myserver-postgres
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: serverdb
      POSTGRES_USER: aedev
      POSTGRES_PASSWORD: aedev123
    volumes:
      - myserver:/var/lib/postgresql/data
    networks:
      - myserver-network

  springboot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: myserver-api
    restart: unless-stopped
    ports:
      - "8086:8086"
    environment:
      # ==============================
      # Spring Boot
      # ==============================
      SERVER_PORT: 8086

      # ==============================
      # PostgreSQL
      # ==============================
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/serverdb
      SPRING_DATASOURCE_USERNAME: aedev
      SPRING_DATASOURCE_PASSWORD: aedev123

      # ==============================
      # Gmail SMTP
      # ==============================
      EMAIL_USER: ${EMAIL_USER}
      EMAIL_PASS: ${EMAIL_PASS}

      # ==============================
      # PayMongo
      # ==============================
      PAYMONGO_SECRET_KEY: ${PAYMONGO_SECRET_KEY}
      PAYMONGO_WEBHOOK_SECRET: ${PAYMONGO_WEBHOOK_SECRET}
      PAYMONGO_LIVE_MODE: ${PAYMONGO_LIVE_MODE}
      PAYMONGO_API_URL: ${PAYMONGO_API_URL}

      # ==============================
      # Frontend
      # ==============================
      FRONT_END_URL: ${FRONT_END_URL}

      # ==============================
      # Jwt Secret
      # ==============================
      JWT_SECRET: ${JWT_SECRET}

      # ==============================
      # ElevenLabs TTS
      # ==============================
      ELEVENLABS_API_KEY: ${ELEVENLABS_API_KEY}
      ELEVENLABS_VOICE_ID: ${ELEVENLABS_VOICE_ID}
      ELEVENLABS_MODEL_ID: ${ELEVENLABS_MODEL_ID}
      ELEVENLABS_API_URL: ${ELEVENLABS_API_URL}

      # ==============================
      # Audio Storage
      # ==============================
      AUDIO_STORAGE_PATH: /app/uploads/audio
      AUDIO_PUBLIC_BASE_URL: ${AUDIO_PUBLIC_BASE_URL}

      # ==============================
      # App API Key (X-App-Key header)
      # ==============================
      APP_API_KEY: ${APP_API_KEY}

      # ==============================
      # Semaphore SMS
      # ==============================
      SEMAPHORE_API_KEY: ${SEMAPHORE_API_KEY}
      SEMAPHORE_API_URL: ${SEMAPHORE_API_URL}
      SEMAPHORE_SENDER_NAME: ${SEMAPHORE_SENDER_NAME}

      # ==============================
      # Cohere chatbot RAG
      # ==============================
      COHERE_API_KEY: ${COHERE_API_KEY}

    volumes:
      - myserver-audio:/app/uploads/audio

    depends_on:
      - postgres
    networks:
      - myserver-network

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: myserver-cloudflared
    restart: unless-stopped
    command: tunnel --no-autoupdate run --token ${CLOUDFLARE_TUNNEL_TOKEN}
    depends_on:
      - springboot
    networks:
      - myserver-network

volumes:
  myserver:
  myserver-audio:

networks:
  myserver-network:
    driver: bridge